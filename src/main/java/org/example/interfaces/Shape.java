package org.example.interfaces;

public interface Shape {
    abstract double calculateArea();
    // Abstract -> they have no implementation, other classes @Override this method and add an implementation
}
